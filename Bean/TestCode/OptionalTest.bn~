use api.Console;
use api.Math;
use api.Delegate;
use api.Optional;

var x = null;
Optional
	.ofNullable(x)
	.ifPresent(def(nox) -> { Console.writeLine(nox); })
	.orElse(def(nox) -> { Console.writeLine("null‚Å‚·"); });
