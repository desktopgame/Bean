use api.Console;
use api.String;
use api.Array;
use api.geom.Vector2D;
use api.test.Ext;

var t = "Ext: " + "Hoge";
Console.writeLine(t);
var inst = Ext.new("Hoge");
Console.writeLine(inst.getValue());
